// Firebase Admin SDK (runs on server — API routes only)
// - initializeApp with service account env vars
// - verifyIdToken (validate JWT from client, extract phone/uid)
// - Used by auth middleware to protect API routes
