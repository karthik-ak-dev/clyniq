// Patient DB queries
// - findById, findByDoctorAndPhone
// - findOrCreate (during booking — upsert by doctor_id + phone)
// - search (by name ILIKE or phone LIKE, scoped to doctor_id)
// - listByDoctor (paginated)
