// Doctor dashboard home — /dashboard
// - Today's date + appointment count
// - List of today's appointments (time, patient name, complaint, fill status badge)
// - Patient search bar
// - Quick stats: total patients count
// - Uses components/dashboard/AppointmentCard, StatsCard, PatientSearchBar
