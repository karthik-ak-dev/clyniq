import { AddPatientForm } from "@/components/dashboard/add-patient-form";

export const metadata = {
  title: "Add New Patient — DoctorRx",
};

export default function AddPatientPage() {
  return <AddPatientForm />;
}
