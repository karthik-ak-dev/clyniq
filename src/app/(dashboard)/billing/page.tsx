// Subscription & billing — /billing
// - If trial: "Trial ends in X days" + "Subscribe Now" button
// - If active: "Next billing: [date]" + "Cancel Subscription"
// - If expired: "Subscription expired" + "Resubscribe"
// - Subscribe button → creates Razorpay subscription → opens checkout
