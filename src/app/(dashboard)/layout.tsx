// Dashboard layout — auth-protected
// - Sidebar nav: Dashboard, Patients, Settings, Billing
// - Header: doctor name, logout button
// - Subscription middleware: if expired → redirect to /billing
// - Wraps all (dashboard)/* pages
