// GET /api/doctor/profile — get logged-in doctor's full profile
// PUT /api/doctor/profile — update profile fields
// - Auth required
// - GET: returns doctor + schedules + subscription status
// - PUT: validates with updateProfileSchema, updates doctor row
