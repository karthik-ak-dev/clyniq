// GET /api/subscription/status
// - Auth required (doctor)
// - Returns current subscription: status, trial_ends_at, period dates
// - Used by dashboard + settings to show trial/active/expired state
